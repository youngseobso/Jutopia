# docker container 내의 설정
systemLog:
  destination: file
  path: "/var/log/mongodb/mongod.log"
  logAppend: true

storage:
  dbPath: "/data/db"

net:
  port: 27017
  bindIp: "0.0.0.0"

replication:
  replSetName: "mongoRepl"

security:
  keyFile: "/etc/mongodb.key"
  authorization: "enabled"